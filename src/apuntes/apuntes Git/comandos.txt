configuracion global

git config --global user.name "jesus"

git config --global user.email "jesus8347@gmail.com"

git config --global color.ui auto

////

git init --- Inicia repositorio

git clone[url] --- Copia un repositorio

git add . --- Toma captura de pantalla a todo en el repositorio

git commit -m "fist commit" --- Gurda los cambios

git checkout -- . --- Devuleve todo lo borrado a la normalidad